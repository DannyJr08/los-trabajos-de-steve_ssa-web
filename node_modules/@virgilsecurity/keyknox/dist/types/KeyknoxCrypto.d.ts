import { IKeyknoxCrypto } from './IKeyknoxCrypto';
import { ICrypto, IPrivateKey, IPublicKey } from './types';
export declare class KeyknoxCrypto implements IKeyknoxCrypto {
    private readonly crypto;
    constructor(crypto: ICrypto);
    decrypt(metadata: string, encryptedData: string, privateKey: IPrivateKey, publicKeys: IPublicKey | IPublicKey[]): string;
    encrypt(data: string, privateKey: IPrivateKey, publicKeys: IPublicKey | IPublicKey[]): {
        metadata: string;
        encryptedData: string;
    };
}
