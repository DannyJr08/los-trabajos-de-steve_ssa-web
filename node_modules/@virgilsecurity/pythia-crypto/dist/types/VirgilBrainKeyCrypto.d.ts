/// <reference types="node" />
import { Data, IBrainKeyCrypto } from './types';
export declare class VirgilBrainKeyCrypto implements IBrainKeyCrypto {
    blind(password: Data): {
        blindedPassword: Buffer;
        blindingSecret: Buffer;
    };
    deblind(options: {
        transformedPassword: Data;
        blindingSecret: Data;
    }): Buffer;
}
