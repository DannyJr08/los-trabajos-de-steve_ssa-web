import { IPythiaClient } from './IPythiaClient';
import { Data, ICrypto, IBrainKeyCrypto } from './types';
export declare class BrainKey {
    private readonly crypto;
    private readonly brainKeyCrypto;
    private readonly pythiaClient;
    private readonly keyPairType?;
    constructor(options: {
        crypto: ICrypto;
        brainKeyCrypto: IBrainKeyCrypto;
        pythiaClient: IPythiaClient;
        keyPairType?: unknown;
    });
    generateKeyPair(password: Data, brainKeyId?: string): Promise<import("@virgilsecurity/crypto-types").IKeyPair>;
}
